import React from 'react'

export default function TablesDistricts({
  searchParams,
}: {
  searchParams: { [key: string]: string | string[] | undefined };
})  {
  return (
    <div>TablesDistricts</div>
  )
}
